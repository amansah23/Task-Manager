{
    "compilerOptions": {
    "module": "ES6",
    "jsx": "preserve",
    "baseUrl": "../src" // Adjust if your source files are in a different folder
    },
    "exclude": [
    "node_modules",
    "**/node_modules/*"
    ]
}